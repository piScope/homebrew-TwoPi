
#### Memo for myself... Step to release a new version
```
   brew install -v --env=std piScope/twopi/twopi --build-bottle
   brew bottle piScope/twopi/twopi

   After a relase is made
   #  call brew bump-formula-pr and copy URL

   #  run the following command locally.
   $ brew  bump-formula-pr -f -d -v --no-fork  --no-browse --no-audit --url <URL>  piscope/twopi/<FORMULA>

   # PR is created and Action will run to make a bottle in GitHub.
   # Once it is done. Put a label to pr-pull-twopi

   
   Then update formula
```
